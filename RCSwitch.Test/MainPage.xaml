<Page
    x:Class="RCSwitch.Test.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RCSwitch.Test"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
            <TextBlock Text="Code Group" Style="{ThemeResource CaptionTextBlockStyle}" />
            <TextBox x:Name="codeGroupTB" />
            <TextBlock Text="Device" Style="{ThemeResource CaptionTextBlockStyle}" />
            <TextBox x:Name="codeDeviceTB" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Content="ON" Click="On_Click" Grid.Column="0" Margin="0,5,5,5" HorizontalAlignment="Stretch" />
                <Button Content="OFF" Click="Off_Click" Grid.Column="1" Margin="5,5,0,5" HorizontalAlignment="Stretch" />
            </Grid>
        </StackPanel>
    </Grid>
</Page>
